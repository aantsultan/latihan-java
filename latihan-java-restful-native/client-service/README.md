HOW TO USE CLIENT - SERVICE
--
First, you need to run `backend-service` module via **SpringBootApplication**

Then run test function in package `com.clientservice.ClientServiceTest`

Created By **Aant**